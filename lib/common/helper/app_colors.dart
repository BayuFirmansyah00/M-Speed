import 'package:flutter/material.dart';

class AppColors {
  // ===========================================================================
  // M-SPEED BRAND COLOR PALETTE (Based on Logo & Identity)
  // ===========================================================================

  // 1. Primary (Red)
  static const Color primary = Color(0xFFE30613); // M-Speed Red
  static const Color primaryDark = Color(0xFFB0000A); // M-Speed Dark Red
  static const Color primaryLight = Color(0xFFFF4D57); // M-Speed Light Red

  // 2. Secondary & Accent
  static const Color secondary = Color(0xFFFF5A00); // M-Speed Orange
  static const Color accent = Color(0xFFFFD200); // M-Speed Yellow

  // 3. Background & Surface
  static const Color background = Color(0xFFF7F7F7); // Very Light Grey Background
  static const Color surface = Color(0xFFFFFFFF); // White Surface

  // 4. Text Colors
  static const Color textPrimary = Color(0xFF171717); // Dark Text
  static const Color textSecondary = Color(0xFF666666); // Grey Text
  static const Color textInverse = Color(0xFFFFFFFF); // White Text (on Primary)

  // 5. Border & Divider
  static const Color border = Color(0xFFE0E0E0); // Light Grey Border
  static const Color divider = Color(0xFFF3F4F6); // Very Light Grey Divider

  // 6. Semantic / Status Colors
  static const Color success = Color(0xFF16A34A); // Green
  static const Color warning = Color(0xFFF59E0B); // Orange/Amber
  static const Color error = Color(0xFFDC2626); // Red Error (Slightly different from Brand Red)
  static const Color info = Color(0xFF2563EB); // Blue

  // ===========================================================================
  // M-SPEED BUYER THEME COLOR PALETTE (Blue B2B)
  // ===========================================================================
  
  static const Color buyerPrimary = Color(0xFF1565C0);
  static const Color buyerDark = Color(0xFF0D47A1);
  static const Color buyerMedium = Color(0xFF1976D2);
  static const Color buyerLight = Color(0xFFE3F2FD);
  static const Color buyerVeryLight = Color(0xFFF5F9FF);
}
